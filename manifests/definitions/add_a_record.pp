define dyn_module::add_a_record(
  $customer_name, 
  $user_name,
  $password,
  $zone="$domainname",
  $dynect_record="$fqdn",
  $dynect_ip="$ipaddress",
  ) {

  add_a_record($customer_name, $user_name, $password, $zone, $dynect_record, $dynect_ip)

}

define dyn_module::add_address_to_load_balance(
  $customer_name,
  $user_name,
  $password,
  $zone="$domainname",
  $dynect_record="$fqdn",
  $dynect_ip="$ipaddress",
  ) {

  add_address_to_load_balance($customer_name, $user_name, $password, $zone, $dynect_record, $dynect_ip)

}

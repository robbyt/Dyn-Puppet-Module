define dyn_module::update_failover_address(
  $customer_name,
  $user_name,
  $password,
  $zone="$domainname",
  $dynect_record="$fqdn",
  $dynect_ip="$ipaddress"
  ) {

  update_failover_address($customer_name, $user_name, $password, $zone, $dynect_record, $dynect_ip)

}

